<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.IntegratedQueueSystem" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <color name="primary_color">@color/purple_500</color>

    <color name="text_color">@color/black</color>
    <color name="text_color_secondary">@color/white</color>
    <color name="default_text_color">@color/gray_500</color>

    <color name="holo_green">@android:color/holo_green_dark</color>
    <color name="holo_red">@android:color/holo_red_dark</color>

    <color name="transparent">@android:color/transparent</color>

</resources>